interface:
  display_name: "Reply"
  short_description: "Draft one prompt from settled Side decisions"
  default_prompt: "Use $reply to turn my settled Side decisions into one prompt for the linked parent, then wait without sending it."

policy:
  allow_implicit_invocation: false
