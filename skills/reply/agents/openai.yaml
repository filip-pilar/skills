interface:
  display_name: "Reply"
  short_description: "Turn Side-task decisions into a clear reply"
  default_prompt: "Use $reply to draft the smallest sufficient response to this Side task's linked parent and wait for my approval."

policy:
  allow_implicit_invocation: false
