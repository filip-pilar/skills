interface:
  display_name: "Reply"
  short_description: "Draft a parent-sufficient Side-task reply"
  default_prompt: "Use $reply to draft the shortest reply that preserves my settled intent and any Side-only context the linked parent needs, then wait for my approval."

policy:
  allow_implicit_invocation: false
