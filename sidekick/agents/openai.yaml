interface:
  display_name: "Sidekick"
  short_description: "Understand Codex in plain language"
  default_prompt: "Use $sidekick to immediately explain the newest completed parent response, identify what it needs from me, and draft a reply when useful."

policy:
  allow_implicit_invocation: false
