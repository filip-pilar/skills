# Skill Builder regressions

Run relevant cases in isolated temporary copies. Give the executor only the case's target and **Executor input**, then assess the hidden pass conditions. Keep run artifacts ephemeral and do not append results to these files.
