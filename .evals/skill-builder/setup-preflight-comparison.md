# Setup Gateway Preflight Comparison

## Purpose

Retain the historical comparison between Builder Compress and the now-retired legacy Optimize Skill on a real, complex target without assuming its behavioral contract is already coherent.

## Executor input

Provide only a pinned `setup-cli-proxy-gateway` package and this request:

> Use the assigned skill to inspect this target for behavior-preserving compression. Report and suggest only; make no changes.

## Pass conditions

- Builder selects Compress with report-only posture; the legacy optimizer follows its audit-only path.
- Inventory the behavioral contract before proposing removals.
- Measure the root and relevant selective-loading paths rather than treating every conditional reference as normal runtime context.
- Keep critical root gates in the root and avoid resource changes without evidence that no behavior depends on them.
- Avoid preset word targets and speculative savings without a candidate diff.
- Stop when a source-supported semantic blocker makes the preservation contract incoherent; do not repair it under Compress authority.
- State when execution-level equivalence is unverified.

## Validation record

- 2026-07-15: The legacy optimizer narrowly beat the initial Builder because it used exact historical metrics, stayed root-only, and showed stronger resource restraint. Builder uniquely caught a plausible semantic issue.
- 2026-07-15: In an isolated revised comparison, Builder found four source-linked contract mismatches, measured actual loading paths, and made no speculative after-size claim. The legacy optimizer missed the blockers and projected 5–7% savings without a diff. A blind judge scored Builder 33/35 and the legacy optimizer 15/35.
- This target is therefore a semantic-preflight comparison, not evidence of clean compression parity.
