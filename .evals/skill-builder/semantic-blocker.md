# Compression Semantic-Blocker Control

## Purpose

Verify that Compress detects a material semantic problem without borrowing Improve authority.

## Executor input

Copy only `fixtures/contradictory-release/` into an isolated executor directory and provide:

> Use `$skill-builder` to compress Contradictory Release without changing behavior. Report and suggest only; do not edit.

## Pass conditions

- Select Compress with report-only posture and perform semantic preflight first.
- Support any claimed contradiction or material tension with exact source behavior, not a generic quality checklist.
- Stop the compression recommendation when the accepted contract is not coherent enough to preserve.
- Offer an explicit switch to Improve without silently choosing or encoding the semantic resolution.
- Do not call file-size reduction semantic optimization.
