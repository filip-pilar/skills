# Skill evaluation workspace

Development-only scenarios, evaluator rubrics, validation records, and deterministic regression tests live here by target skill. They remain outside distributable skill packages and fresh executor workspaces; runtime-consumed examples and templates belong with the skill that loads them.
